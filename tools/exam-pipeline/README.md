# Exam Pipeline Tool Migration Target

This directory is the Milestone 8 target for exam data acquisition and transformation. It is README-only until the current exam scripts are packaged.

Near-term migration purpose:

- own the pipeline currently split across `scripts/auto_update_exam_data.py` and `scripts/analyze_and_update.py`;
- generate exam artifacts compatible with the current exam vertical;
- keep generated exam data separate from collection source packages.

The exam vertical is a product experience, not a sitegraph source.

